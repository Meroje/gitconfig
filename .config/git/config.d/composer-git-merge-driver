# https://github.com/balbuf/composer-git-merge-driver
[merge "composer_json"]
	name = composer JSON file merge driver
	driver = $XDG_CONFIG_HOME/git/config.d/composer-git-merge-driver.php %O %A %B %L %P
	recursive = binary
